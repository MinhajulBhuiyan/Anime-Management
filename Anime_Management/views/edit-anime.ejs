<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Anime</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
    <style>
        @font-face {
            font-family: 'Blomberg';
            src: url('/fonts/Blomberg-8MKKZ.otf') format('opentype');
        }

        body {
            background-image: url('/images/editanime-image.png');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
            font-family: 'Blomberg', sans-serif;
            position: relative;
            min-height: 100vh;
        }

        /* Black shade overlay */
        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6); /* 60% opacity for black shade */
            z-index: -1; /* Place it behind the content */
        }

        .form-container {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4);
            text-align: center;
            transition: transform 0.3s ease;
        }

        .form-container:hover {
            transform: scale(1.05);
        }

        .form-container h1 {
            color: #ff4b5c;
            margin-bottom: 1.5rem;
            font-size: 3rem;
            text-transform: uppercase;
            font-weight: bold;
            letter-spacing: 2px;
        }

        .form-container label {
            color: black;
            font-size: 1.2rem;
        }

        .button {
            background-color: #ff4b5c;
            color: white;
            padding: 15px;
            border-radius: 12px;
            width: 100%;
            font-size: 1.1rem;
            transition: background-color 0.3s ease, transform 0.2s ease, box-shadow 0.2s ease;
            font-weight: bold;
            box-shadow: 0 4px 10px rgba(255, 75, 92, 0.5);
        }

        .button:hover {
            background-color: #ff2e42;
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(255, 75, 92, 0.7);
        }

        .button-delete {
            color: #ff4b5c;
            font-size: 1rem;
            font-weight: bold;
        }

        footer {
            margin-top: 20px;
            color: black;
        }
    </style>
</head>
<body class="flex justify-center items-center py-10">
    <div class="form-container w-full max-w-md">
        <h1>Edit Anime</h1>
        <form action="/edit/<%= anime.id %>" method="POST">
            <div class="mb-6">
                <label for="anime_title">Anime Title</label>
                <input type="text" name="anime_title" id="anime_title" value="<%= anime.anime_title %>" required class="border border-gray-300 rounded-lg w-full py-2 px-3 focus:outline-none focus:ring-2 focus:ring-indigo-500">
            </div>
            <div class="mb-6">
                <label for="description">Description (Optional)</label>
                <textarea name="description" id="description" rows="4" class="border border-gray-300 rounded-lg w-full py-2 px-3 focus:outline-none focus:ring-2 focus:ring-indigo-500"><%= anime.description %></textarea>
            </div>
            <button type="submit" class="button">Update Anime</button>
        </form>
        
        <div class="mt-4 text-center">
            <form action="/delete/<%= anime.id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this anime?');">
                <button type="submit" class="button-delete hover:text-red-800 transition">Delete Anime</button>
            </form>
        </div>
        
        <div class="mt-6 text-center">
            <a href="/dashboard" class="text-gray-600 hover:text-indigo-600 transition">Back to Dashboard</a>
        </div>
    </div>
</body>
</html>
