<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
    <style>
        @font-face {
            font-family: 'Blomberg';
            src: url('/fonts/Blomberg-8MKKZ.otf') format('opentype');
        }

        body {
            background-image: url('/images/signup-image.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-attachment: fixed;
            font-family: 'Blomberg', sans-serif;
            position: relative;
            min-height: 100vh;
        }

        body::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            z-index: -1;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.4);
            text-align: center;
            margin: 20px;
            max-width: 800px; /* Limit the max width for better visibility */
        }

        .form-container h2 {
            color: #ff4b5c;
            margin-bottom: 2rem;
            font-size: 2.5rem;
            text-transform: uppercase;
            font-weight: bold;
            letter-spacing: 2px;
        }

        .form-container input {
            background-color: #f2f2f2;
            border: 1px solid #ccc;
            padding: 12px;
            border-radius: 8px;
            transition: all 0.3s ease;
            font-family: Arial, sans-serif;
        }

        .form-container input:focus {
            border-color: #ff4b5c;
            box-shadow: 0 4px 12px rgba(255, 75, 92, 0.5);
        }

        .form-container button {
            background-color: #ff4b5c;
            color: white;
            padding: 15px;
            border-radius: 12px;
            width: 100%;
            font-size: 1.1rem;
            transition: background-color 0.3s ease, transform 0.2s ease;
            font-weight: bold;
            margin-top: 1rem;
        }

        .form-container button:hover {
            background-color: #ff2e42;
            transform: translateY(-2px);
            box-shadow: 0 8px 16px rgba(255, 75, 92, 0.5);
        }

        .form-container a {
            color: #ff4b5c;
            font-weight: bold;
            text-decoration: none;
            transition: color 0.2s ease;
        }

        .form-container a:hover {
            color: #ff2e42;
            text-decoration: underline;
        }

        label {
            margin-bottom: 6px;
            color: #333;
        }

        .input-group {
            margin-bottom: 16px;
        }

        .show-hide-text {
            position: absolute;
            right: 10px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #ff4b5c;
            font-size: 0.9rem;
            padding: 0;
        }

        .favorite-anime-section {
            margin-top: 1rem;
            padding: 20px;
            background: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        @media (min-width: 640px) {
            .form-grid {
                display: grid;
                grid-template-columns: 1fr 1fr;
                gap: 16px;
            }
        }
    </style>
    <script>
        function togglePasswordVisibility(passwordFieldId, toggleButton) {
            const passwordField = document.getElementById(passwordFieldId);
            const isPasswordVisible = passwordField.getAttribute('type') === 'text';

            passwordField.setAttribute('type', isPasswordVisible ? 'password' : 'text');
            toggleButton.textContent = isPasswordVisible ? 'Show' : 'Hide';
        }
    </script>
</head>

<body class="flex justify-center items-center h-screen">
    <div class="form-container w-full">
        <h2>Create an Account</h2>
        <form action="/signup" method="POST">
            <div class="form-grid">
                <div class="input-group">
                    <label for="name">Name</label>
                    <input type="text" name="name" id="name" required class="w-full focus:outline-none pr-12">
                </div>
                <div class="input-group">
                    <label for="email">Email</label>
                    <input type="email" name="email" id="email" required class="w-full focus:outline-none pr-12">
                </div>
                <div class="input-group relative">
                    <label for="password">Password</label>
                    <input type="password" name="password" id="password" required class="w-full focus:outline-none pr-12">
                    <span id="togglePassword" class="show-hide-text" onclick="togglePasswordVisibility('password', this)">Show</span>
                </div>
                <div class="input-group relative">
                    <label for="confirm_password">Confirm Password</label>
                    <input type="password" name="confirm_password" id="confirm_password" required class="w-full focus:outline-none pr-12">
                    <span id="toggleConfirmPassword" class="show-hide-text" onclick="togglePasswordVisibility('confirm_password', this)">Show</span>
                </div>
            </div>

            <div class="favorite-anime-section">
                <h3 class="text-xl font-bold text-gray-700 mb-4">Favorite Anime</h3>
                <div class="form-grid">
                    <div class="input-group">
                        <label for="fav_anime1">Favorite Anime 1</label>
                        <input type="text" name="fav_anime1" id="fav_anime1" class="w-full focus:outline-none pr-12">
                    </div>
                    <div class="input-group">
                        <label for="fav_anime2">Favorite Anime 2</label>
                        <input type="text" name="fav_anime2" id="fav_anime2" class="w-full focus:outline-none pr-12">
                    </div>
                </div>
            </div>

            <button type="submit">Sign Up</button>
        </form>
        <div class="mt-4">
            <p class="text-gray-600">Already have an account? <a href="/login">Login here</a>.</p>
        </div>
    </div>
</body>

</html>
